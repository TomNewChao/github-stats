name: Generate Stats Images

on:
  push:
    branches: 
      - master
  schedule:
    - cron: "5 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

defaults:
  run:
    shell: bash -euxo pipefail {0}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29  # v2 tag
      with:
        version: 0.16.0

    - name: Build
      run: |
        zig build

    - name: Checkout history branch
      run: |
        git config --global user.name "jstrieb/github-stats"
        git config --global user.email "github-stats[bot]@jstrieb.github.io"
        # Push generated files to the generated branch
        git pull
        git checkout generated || git checkout -b generated

    - name: Exclude fork repos (dynamic)
      run: |
        FORKS=$(gh api "users/TomNewChao/repos?per_page=100&type=owner" --paginate \
          --jq '.[] | select(.fork==true) | .name' | paste -sd, -)
        MANUAL="${{ secrets.EXCLUDE_REPOS }}"
        if [ -n "$FORKS" ] && [ -n "$MANUAL" ]; then
          EXCLUDE_ALL="${FORKS},${MANUAL}"
        elif [ -n "$FORKS" ]; then
          EXCLUDE_ALL="${FORKS}"
        else
          EXCLUDE_ALL="${MANUAL}"
        fi
        echo "EXCLUDE_ALL=${EXCLUDE_ALL}" >> $GITHUB_ENV
        echo "Excluded repos: ${EXCLUDE_ALL}"
      env:
        GH_TOKEN: ${{ secrets.ACCESS_TOKEN }}

    - name: Generate images
      run: |
        ./zig-out/bin/github-stats
      env:
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        EXCLUDE_REPOS: ${{ env.EXCLUDE_ALL }}
        EXCLUDE_LANGS: ${{ secrets.EXCLUDE_LANGS }}
        EXCLUDE_PRIVATE: "false"
        SILENT: "true"
        # TODO: Remove this when they get their API working again
        # https://github.com/orgs/community/discussions/192970
        MAX_RETRIES: 5

    - name: Commit to the repo
      run: |
        git add .
        # Force the build to succeed, even if no files were changed
        git commit -m 'Update generated files' || true
        git push --set-upstream origin generated
